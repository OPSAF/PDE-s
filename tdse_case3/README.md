Case 3 — Constant Force Potential

This folder contains `case3.py` which simulates the 1D TDSE with a constant force potential V(x)=F*x
and compares the numerical split-step spectral solution with the analytic accelerated Gaussian solution.

Run:

python case3.py

Outputs: plots, convergence data and `animation.mp4`.
